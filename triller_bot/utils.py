from .downloader import Downloader
#import hmac

class Utils:
    
    Downloader = Downloader
    
    """    def genPnHash(n1, n2):
        return str(hmac.new(n1.encode(), n2.encode(), "sha256").hexdigest())"""
        